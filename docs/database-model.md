# Модель данных

Проект использует пять предметных и одну служебную схему PostgreSQL.

## Предметная схема `aviation`

Содержит восемь сущностей:

1. `airports` — аэропорты;
2. `aircrafts` — типы воздушных судов;
3. `seats` — места и классы обслуживания;
4. `flights` — рейсы;
5. `bookings` — бронирования;
6. `tickets` — билеты пассажиров;
7. `ticket_flights` — сегменты маршрута, связывающие билет и рейс;
8. `boarding_passes` — посадочные талоны.

Таблица `ticket_flights` разрешает связь многие-ко-многим между билетами и
рейсами. `boarding_passes` зависит от составного ключа сегмента билета, поэтому
посадочный талон нельзя создать для несуществующей пары «билет — рейс».

Начальное наполнение содержит 10 аэропортов, 5 типов самолётов, 900 мест,
50 000 рейсов, 250 000 бронирований, 500 000 билетов, 1 000 000 сегментов и
около 800 000 посадочных талонов.

## Предметная схема `retail`

Содержит 10 сущностей розничной торговли: клиентов, адреса, категории, товары,
магазины, складские остатки, заказы, позиции заказов, платежи и отгрузки. В ней
примерно 1,34 млн строк. Она вместе с `aviation` используется для обучения
многодоменных моделей.

## Предметная схема `logistics`

Содержит 10 сущностей цепочки поставок: поставщиков, объекты, товары, заказы на
закупку и их строки, перевозчиков, отправки и их состав, события отслеживания и
движения запасов. В ней 2 275 050 строк. Схема исключена из обучения и служит
для строгой проверки leave-one-database-out.

## Предметная схема `chbenchmark`

Содержит 12 сущностей гибридной системы заказов: регионы, страны, поставщиков,
склады, районы, клиентов, товары, остатки, заказы, новые заказы, строки заказов
и историю платежей. Детерминированное наполнение включает примерно 1,2 млн
строк. Структура совместима с семантикой открытого CMU BenchBase
CH-benCHmark, а обучающие и контрольные SELECT-шаблоны проекта разделены.

## Предметная схема `pagila`

Изолированная копия открытой учебной БД Pagila v3.1.0 моделирует видеопрокат.
Она содержит 15 основных сущностей: фильмы, актёров, категории, языки,
инвентарь, прокаты, платежи, клиентов, сотрудников, магазины и адресный
справочник. Секционированная таблица `payment` дополнительно имеет семь
физических секций. В схеме находятся 16 044 проката и 16 049 платежей; для неё
реализованы 30 собственных сложных шаблонов запросов. Pagila используется как
development-домен и не входит в запечатанные внешние контроли.

## Служебная схема `pqo`

Содержит данные системы оптимизации:

- `query_run` — отдельные запуски SQL-запросов;
- `query_features` — статические признаки SQL;
- `execution_plan` — JSON-планы и их агрегированные характеристики;
- `model_prediction` — прогнозы версий моделей;
- `index_recommendation` — рекомендации по индексам;
- `recommendation_audit` — история изменения статуса рекомендаций.

Такое разделение не смешивает предметные данные с данными эксперимента и
упрощает перенос оптимизатора на другую пользовательскую БД.

